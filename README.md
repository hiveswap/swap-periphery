# HiveSwap Router

### Bsc-Test

The following assumes the use of `node@>=10`.

## Install Dependencies

`npm i`

## Compile Contracts

`truffle compile`

## deploy Contracts
`truffle migrate --network=map_main`

